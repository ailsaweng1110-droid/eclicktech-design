# 交互内容可爬取性核查清单

## 使用说明

本清单归入 Step 6 设计侧「关键文字可爬取性」评分使用。
**判断原则：有交互不扣分，交互内容未落地 HTML 才扣分。**

CSS 动画、视差滚动、Hover 效果、数字滚动、入场动画等纯样式交互不影响评分。
只有"关键内容因交互而对爬虫不可见"才扣分。

---

## 核查项目

### 1. Tab 切换内容
- **检查方式**：查看页面源码，搜索非激活 Tab 里的关键词
- ✅ 合格：非激活 Tab 的文字存在于 HTML 源码中（`display:none` 隐藏但内容在）
- ❌ 不合格：切换 Tab 后内容才出现，源码里找不到对应文字（JS 动态注入）
- **扣分**：-2 分

### 2. 折叠 / Accordion 区块
- **检查方式**：查看折叠状态下的源码是否包含折叠内容
- ✅ 合格：折叠内容在 HTML 中（推荐使用 `<details>/<summary>` 原生标签）
- ❌ 不合格：折叠展开后才动态加载内容
- **扣分**：-1～2 分（视内容重要程度）

### 3. 弹窗 / Modal
- **检查方式**：判断弹窗里是否含核心公司信息、产品说明
- ✅ 合格：弹窗内容是次要信息（如联系表单、视频播放器）
- ❌ 不合格：公司核心介绍、核心产品描述藏在需点击才出现的弹窗里
- **扣分**：-2 分

### 4. 视频 / 动画 Banner
- **检查方式**：查看视频区域是否有对应的静态文字描述
- ✅ 合格：有静态文字副本（即使用 CSS 隐藏，AI 仍可读）
- ❌ 不合格：Banner 区域纯视频，无任何文字描述
- **扣分**：-1～2 分（视 Banner 承载的信息量）

### 5. Canvas / WebGL 渲染文字
- **检查方式**：判断页面是否有用 Canvas 绘制的文字内容
- ✅ 合格：Canvas 只用于图形装饰，文字信息有 HTML 对应
- ❌ 不合格：重要文字通过 Canvas 渲染，HTML 里无对应内容
- **扣分**：-2 分

### 6. 滚动懒加载关键段落
- **检查方式**：查看源码是否有 `data-src`、`loading="lazy"` 等属性应用于文字区块
- ✅ 合格：懒加载只用于图片，文字内容正常渲染
- ❌ 不合格：核心段落（如产品介绍、公司描述）需滚动到视区才注入 DOM
- **扣分**：-1～2 分

### 7. SPA 客户端路由内容
- **检查方式**：直接 `web_fetch` 子页面 URL，判断是否能获取到内容
- ✅ 合格：子页面有 SSR / SSG，直接访问 URL 即可获取完整内容
- ❌ 不合格：子页面内容只在客户端路由后才渲染（直接访问 URL 拿到空壳）
- **扣分**：直接影响技术侧"渲染方式"评分项，判为 3 分以下，并列为 P0

---

## 验收方法（供开发团队使用）

浏览器右键「查看页面源代码」（非「检查元素」），搜索 Tab 或折叠区块里的关键文字：
- 能搜到 → 内容已在 HTML 里，合格
- 搜不到 → JS 动态注入，需要修改实现方式
