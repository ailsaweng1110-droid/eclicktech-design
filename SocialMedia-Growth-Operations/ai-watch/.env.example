# 复制此文件为 .env，填入真实值（.env 已 gitignore）
#   cp .env.example .env

# 飞书多维表格 webhook 地址（在 飞书 Base → 自动化 → 接收到 Webhook 时 → 复制）
FEISHU_WEBHOOK_URL=

# 每个栏目最多推送多少条候选给飞书（防止刷爆免费版自动化额度）
MAX_PER_CATEGORY=8

# 推送间隔（秒），防止触发飞书限速
PUSH_DELAY_SECONDS=0.5

# 时间窗口（天）
WINDOW_DAYS=7
